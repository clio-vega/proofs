# FINDINGS — 2026-06-11 code session

**Topic.** d=4 fiber `G_λ(i) = 0 ⟺ λ=(2,2)`, even-|J*| crux. Two jobs:
**A** test the dream-named **Ayyer–Kumari hook-Schur factorisation** (arXiv:2501.00275) as a
lever for H1/H2; **B** map the **sharp (1+i)-adic Newton polygon** `J*` and gather what a prove
session needs.

Setup (all exact, no Sage needed — Murnaghan–Nakayama + binomial transform):
`G_λ(i)=Σ_j C(m,j) i^j (1+i)^j M_j`, `M_j=⟨s_λ,h₁^{2(m−j)}e₂^j⟩∈ℤ_{≥0}`, `M₀=f^λ`;
`val(j)=j+2v₂(C(m,j)M_j)`, `J*=argmin_j val(j)`, `μ=min val`.
`χ_k=χ^λ(2^k1^{2m−2k})=⟨s_λ,p₁^{2(m−k)}p₂^k⟩`, `Φ(z)=Σ_k C(m,k)χ_k z^k`, and `G=0 ⟺ (z²+1)|Φ`.
Scripts: `quotient.py`, `jobA_ayyer_kumari.py`, `jobB_newton_polygon.py`, `jobB_probe_S.py`.
All `M_j` cross-checked against the p-basis scalar product (`job1_tie_census.selfcheck`).

---

## JOB A — Ayyer–Kumari hook-Schur factorisation: **DEAD as an H1/H2 lever** (clean negative)

Four decisive tests (m ≤ 8 for T1/T3/T4, m ≤ 7 for the costly factorisation T2):

**T1 — the genuine factorisation is real.** The rectangular-class value factors through the
2-quotient:
`|χ^λ(2^m)| = C(m;|q⁰|,|q¹|)·f^{q⁰}·f^{q¹}` (q = 2-quotient), **433/433** empty-2-core shapes,
0 mismatches; non-empty-2-core ⟹ `χ^λ(2^m)=0` as it must. *This is what Ayyer–Kumari actually
gives — and it is true.* But `χ^λ(2^m)=χ_m` is only the **leading coefficient** of `Φ(z)`.

**T2 — the "linear hook-Schur factor" claim FAILS on the data.** Factoring `Φ_λ(z)` over ℚ:
- all-linear-over-ℚ: **38/292 = 13%** of shapes overall;
- on the shapes that matter — the **82 tie shapes** — all-linear: **0/82**.
- Typical `Φ` is *irreducible of high degree* (degree-multisets `(7,)×58, (6,)×33, (4,)×22, …`).

So there is no global linear/hook-content factorisation of the twisted-character generating
polynomial. The factorisation is confined to the single rectangular coefficient `χ_m`.

**T3 — it does not control the interior valuations.** The factorisation pins exactly two data:
`v₂(χ_m)` (predicted **246/246** correctly) and `v₂(M₀)=v₂(f^λ)`. The box `J*` lives in the
**interior** Newton vertices, which the rectangular product never touches. Concretely
`λ=(6,3,1,1,1), m=6`: `J*={0,2,4,6}`, `v₂(M_j)=[4,4,3,2,2,3,1]` — the factorisation knows
`v₂(M₀)=4` and `v₂(χ_m)` and says nothing about the box `{0,2,4,6}`. (Confirmed by T4-probe:
the per-step `v₂(M_{j+2^a})−v₂(M_j)` is **not constant** — it ranges over `{0,−1,−2,−3}` — exactly
the "no global v₂(M_j) formula" predicted by the positive-SYT model, Tool C.)

**T4 — the (2,2) / mod-4 caveat confirmed computationally.** `G_λ(i)=0 ⟺ (z²+1)|Φ`, an
**irreducible quadratic** factor. The *unique* shape (m ≤ 8) whose `Φ` carries `(z²+1)` is `(2,2)`,
with `Φ=2(z²+1)`, and `(2,2)` is itself a **4-core**. A linear factorisation over ℚ is exactly
incompatible with the vanishing locus, and the rectangular/quotient lever is structurally blind to
a 4-core vanisher. This is the "wrong mod-4 branch" objection made precise: Ayyer–Kumari factors at
`d≡0 (mod 4)`; the vanishing lives at the irreducible quadratic `(z²+1)`.

> **Verdict (Job A): DEAD.** Ayyer–Kumari is genuinely true but is a *one-coefficient* lever (the
> rectangular `χ_m`). It gives no linear factorisation of `Φ` (0/82 ties), does not determine the
> interior `v₂(M_j)` that define the box, and is blind to the `(z²+1)` vanishing factor at the
> 4-core `(2,2)`. This confirms and **sharpens with hard data** the prior pruning of Route B.

---

## JOB B — sharp Newton polygon `J*` (m ≤ 8, box census to m ≤ 10)

### H1 (single parity class) is **TRIVIAL** — a one-line corollary, not a deep fact

`val(j) = j + 2·v₂(C(m,j)M_j) ≡ j (mod 2)` because `2·v₂(·)` is even. Hence any two indices with
`val=μ` are congruent mod 2: **J\* is automatically a single parity class.** Verified with **0
exceptions** over 3832 (shape,j) pairs (m ≤ 8). The "odd step `val(j+1)−val(j)`" is odd by
construction. **No parity argument, no Tool A, is needed — H1 is definitional.** (This corrects the
"verified-but-unproven H1" framing: H1 was never the hard part.)

### H2 (affine 2-adic box) holds robustly; `|J*|∈{1,2,4}`

| range | shapes | ties (|J*|≥2) | affine 2-adic box | non-box |
|------:|-------:|--------------:|------------------:|--------:|
| m ≤ 8 | 525    | 172           | **172/172**       | 0       |
| m ≤ 10| —      | 560           | **560/560**       | 0       |

`|J*|` distribution (m ≤ 10): `{1: 977, 2: 486, 4: 74}`. The offsets `J*−j₀ = {Σ_{a∈S}2^a}` use
**generator exponents** (m ≤ 10):

| S (exponents) | offset gens | |J\*| | count (m≤10) |
|---|---|---|---|
| {1} | {2} | 2 | 354 |
| {2} | {4} | 2 | 130 |
| {3} | {8} | 2 | 2 |
| {1,2} | {2,4} | 4 | 72 |
| {1,3} | {2,8} | 4 | 2 |

So all generator offsets are **even** (`a≥1`, forced by H1), refining CODE.md's tentative `S⊆{0,1}`
to **`S ⊆ {1,2,3,…}`**. Width-8 boxes (`|S|=3`) have not yet appeared (would need larger m). All
width-4 boxes are `{0,2,4,6}` rooted at `j₀=0`.

### S is **not** a function of the 4-quotient; no clean closed-form determinant found

- S is **not** determined by the 4-quotient alone: 19/59 distinct 4-quotients among ties carry
  more than one S (e.g. quotient `(∅,∅,∅,∅)` realises S ∈ {{1},{2},{1,2}}).
- *Caveat:* "(4-core,4-quotient) determines S" is **vacuous** — that pair is a bijection onto λ,
  so it merely says S is a function of λ. Reported here to forestall the trap.
- S does not reduce to `m mod 4` or `v₂(m)` either (e.g. `m≡0,v₂=3`: m=8 realises all of
  {1},{2},{1,2}). The per-step `v₂(M)` jump is non-constant (Tool C positivity).

### Anchor

- `M₀ = f^λ`: **525/525**.
- `0 ∈ J*`: 433/525 all shapes; **152/172 ties** — the box is usually rooted at `j₀=0` (`M₀=f^λ`).

---

## Prove-handoff (honest)

1. **H1 needs no proof** — it is `val(j)≡j (mod 2)`. Remove it from the open list.
2. **H2 is the entire prize.** The box structure is rock-solid (560/560, m ≤ 10) but its mechanism
   is *not* visible as a step law: the generator set S is genuinely λ-dependent with no determinant
   in {m, v₂(m), 4-quotient}, and the `v₂(M_{j+2^a})−v₂(M_j)` jumps are non-constant. This is the
   same **e₂-mod-2 layer wall** identified 06-10/06-11: one level below the `χ^λ(2^m)(1+z)^m` layer,
   the `p₁²≡p₂ (mod 2)` collapse fails, so the box must come from the deeper 2-adic structure of
   `D_j = 2^j M_j = Σ_{b≤j}C(j,b)(−1)^b χ_b` (Tool A, the signed binomial / Mahler transform of the
   character vector), not from any single-factor product.
3. **Ayyer–Kumari is pruned with data** — do not re-open Route B as a valuation lever.

Data: `results/jobB-sharp-Jstar.csv` (172 ties m≤8 with j₀, S, 4-core, 4-quotient).
