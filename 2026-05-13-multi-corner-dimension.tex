\documentclass[11pt]{article}
\usepackage{amsmath,amsthm,amssymb}
\usepackage[margin=1in]{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{booktabs}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{conjecture}[theorem]{Conjecture}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{example}[theorem]{Example}

\title{A recursive dimension formula for $B_{\ell+1}^{(\lambda)} V_\lambda$\\
  at multi-corner partitions}
\author{Clio}
\date{13 May 2026}

\begin{document}
\maketitle

\begin{abstract}
Let $H_q(S_n)$ be the Iwahori--Hecke algebra and $V_\lambda$ its Specht
module in the Hoefsmit seminormal basis. Write
$R'_j = (T_{j-1}{+}1)\cdots(T_1{+}1)$ and
$B_{\ell+1}^{(\lambda)} V_\lambda := R'_{\ell+1}\cdots R'_n\, V_\lambda$ at
the sharp index $\ell+1$, where $\ell = \ell(\lambda)$. We propose and
test --- \emph{in the stable regime of $\lambda$ having sufficiently
many trailing $1$-rows} --- a recursive formula expressing
$\dim B_{\ell+1}^{(\lambda)} V_\lambda$ as a sum over length-preserving
corners $c_i$ of $\lambda$:
\[
   \dim B_{\ell+1}^{(\lambda)} V_\lambda
   \;=\;
   \sum_{i=1}^{r}\,
   \dim B_{\ell-1}^{(\mu_i)} V_{\mu_i},
   \qquad
   \mu_i := \lambda \setminus \{c_i, c_*(\lambda)\},
\]
where $c_*(\lambda) = (\ell, 1)$ is the column-$1$ bottom and $r$ is the
number of length-preserving corners. The base case ($r = 0$, i.e.\ a hook
or near-hook) is dim~$=1$. Combinatorially, $\dim B_{\ell+1}^{(\lambda)}
V_\lambda$ counts saturated descending chains
$\lambda \supset \lambda^{(1)} \supset \cdots \supset \text{(hook)}$ in
which each step removes the column-$1$ bottom plus one length-preserving
corner.

We verify the formula computationally on nine families covering
$r \in \{1, 2\}$ at $n \in \{8, \ldots, 11\}$ with $q \ge 2$ trailing
$1$-rows, all matching. The two-corner family is inductively closed in
this regime: every dimension is reduced to smaller cases of the same
recursion, bottoming out at hooks. \emph{Outside} the stable regime,
new data at $\lambda = (4, 3, 2, 1)$, $n = 10$ ($q = 0$, $r = 3$) gives
$\dim = 24 \ne 16 = f^{(3, 2, 1)}$: the closed form fails and the
recursion's descent step is no longer well-defined. The precise
stability threshold is open; empirically $q \ge 2$ suffices on every
tested shape, and $q = 1$ is untested.

\emph{Closed form (stable regime).} In the \emph{stable regime} ---
defined below as having enough trailing $1$-rows that the recursion
descends cleanly to hooks --- the recursion admits a clean closed-form
solution via a bijection between saturated descending chains and
standard Young tableaux of the \emph{decoration shape}
$\lambda^{(\ge 2)} := (\lambda_1 - 1, \lambda_2 - 1, \ldots)$
(the partition obtained from $\lambda$ by deleting column~$1$). The
main result is
\[
   \boxed{\;\dim B_{\ell + 1}^{(\lambda)} V_\lambda \;=\; f^{\lambda^{(\ge 2)}}
   \quad\text{(stable regime, conjectural $q \ge 2$)}\;}
\]
where $f^\mu$ is the number of SYT of shape~$\mu$. By the hook-length
formula this gives an explicit closed form via the hook product on the
decoration shape.

\emph{Non-stable warning.} New computational data at $\lambda = (4,3,2,1)$
with $n = 10$ (zero trailing $1$-rows, $q = 0$) gives
$\dim = 24 \ne 16 = f^{(3,2,1)}$. The closed-form formula
\emph{fails} outside the stable regime; the recursion itself breaks down
because its descent step requires $\lambda_\ell = 1$ to define $c_*$,
and removing $\{c_i, c_*\}$ can produce a $\mu_i$ whose last row exceeds
$1$. Empirically, all $9$ tested shapes with $q \ge 2$ trailing $1$-rows
satisfy the closed form; $q = 1$ is untested; $q = 0$ fails. The precise
stability threshold is open.

The proof sketch generalises the May-12 three-branch reduction to a
$\binom{r+1}{2}$-branch decomposition. Pairs of length-preserving
corners contribute zero by a $j$-formula leakage argument; pairs of the
form $\{c_i, c_*\}$ contribute the next stage of the recursion;
independence follows from position-of-$n$ and position-of-$(n-1)$
projections on the Hoefsmit seminormal basis.
\end{abstract}

\section{Setup}\label{sec:setup}

\subsection{Notation}

Let $q$ be generic. The \emph{Iwahori--Hecke algebra} $H_q(S_n)$ has
generators $T_1, \ldots, T_{n-1}$ subject to $(T_i - q)(T_i + 1) = 0$ and
the braid relations $T_i T_{i+1} T_i = T_{i+1} T_i T_{i+1}$,
$T_i T_j = T_j T_i$ for $|i-j| \ge 2$. For each $\lambda \vdash n$, write
$V_\lambda$ for the Specht module in the Hoefsmit seminormal basis
$\{v_T : T \in \mathrm{SYT}(\lambda)\}$. The $q$- and $(-1)$-eigenspaces of
$T_j$ are denoted $E_j^+$ and $E_j^-$.

Write
\[
   R'_j \;:=\; (T_{j-1}{+}1)(T_{j-2}{+}1)\cdots(T_1{+}1) \;\in\; H_q(S_j),
\]
and, for $\lambda \vdash n$ with $\ell := \ell(\lambda)$,
\[
   B_{j}^{(\lambda)} V_\lambda \;:=\; R'_j R'_{j+1}\cdots R'_n\, V_\lambda.
\]
We focus on the \emph{sharp} index $j = \ell + 1$:
\[
   B_{\ell+1}^{(\lambda)} V_\lambda \;=\; R'_{\ell+1} R'_{\ell+2}\cdots R'_n\, V_\lambda.
\]

\subsection{Corners}

A cell $c = (r, \lambda_r)$ is a \emph{removable corner} of $\lambda$ if
$\lambda \setminus \{c\}$ is still a partition. We single out two types.

\begin{definition}[Length-preserving corner]
A removable corner $c = (r, \lambda_r)$ is \emph{length-preserving} if
$r < \ell(\lambda)$ and $\lambda_{r+1} < \lambda_r$. Equivalently, removing
$c$ leaves $\ell(\lambda)$ unchanged.
\end{definition}

\begin{definition}[Column-$1$ bottom]
The \emph{column-$1$ bottom} of $\lambda$ is
$c_*(\lambda) := (\ell(\lambda), 1)$. It is always removable (when
$\lambda \ne (1^n)$); removing it decreases $\ell$ by~$1$.
\end{definition}

If $\lambda$ has $r$ length-preserving corners $c_1, \ldots, c_r$, then it
has exactly $r + 1$ removable corners in total: the $c_i$ together with
$c_*$. (For hooks, $r = 0$; for staircases-like shapes, $r$ can be large.)

\subsection{Background}

The starting point is the \emph{rank-zero theorem} (Clio,
2026-05-08--10):
\[
   \Pi^{S_n}|_{V_\lambda} \;=\; 0 \quad \text{whenever } \ell(\lambda) > \lceil n/2 \rceil,
\]
where $\Pi^{S_n} = B_2^{(\lambda)}$. Subsequent work (the
\emph{$j$-formula refinement}, Clio 2026-05-11) showed that at the sharp
index $\ell + 1$,
\[
   B_{\ell+1}^{(\lambda)} V_\lambda \;\subseteq\; E_1^-\!\mid_{V_\lambda},
\]
and is genuinely the bottom non-vanishing stage of the iterated
product. The May-11 \emph{rank-corner formula} conjectured
\[
   \dim B_{\ell+1}^{(\lambda)} V_\lambda \;\stackrel{?}{=}\; \#\{\text{length-pres corners of }\lambda\} \;=\; r.
\]
This was refuted by Clio (2026-05-12, evening~3, paper
\texttt{2026-05-12-evening-4-2-1n-dim3.tex}) at
$\lambda = (4, 2, 1^{n-6})$: the prediction is $r = 2$ but the actual
dimension is $3$. The proof there established a three-branch reduction
yielding $\dim = \dim B^{(\mu_A)} + \dim B^{(\mu_B)} = 2 + 1 = 3$, where
$\mu_A, \mu_B$ are obtained by removing $c_*$ together with the two
length-preserving corners. This suggested a \emph{recursive} replacement
for the rank-corner formula, which we develop here.

\section{The refined formula}\label{sec:formula}

\begin{conjecture}[Refined dimension formula, stable regime]\label{conj:main}
Let $\lambda \vdash n$ be rank-zero with $\ell = \ell(\lambda)$ and
length-preserving corners $c_1, \ldots, c_r$ ($r \ge 0$), and column-$1$
bottom $c_*(\lambda)$. \emph{Assume $\lambda$ lies in the stable regime}
(Definition~\ref{def:stable} below; empirically $q(\lambda) \ge 2$
suffices on tested shapes). Then
\begin{equation}\label{eq:main}
   \dim B_{\ell+1}^{(\lambda)} V_\lambda
   \;=\;
   \sum_{i=1}^{r}\,
   \dim B_{\ell-1}^{(\mu_i)} V_{\mu_i},
   \qquad
   \mu_i \;:=\; \lambda \setminus \{c_i, c_*(\lambda)\}.
\end{equation}
The base case ($r = 0$, $\lambda$ a hook) is $\dim = 1$.
\end{conjecture}

Note that $\mu_i$ is a partition of $n - 2$ with
$\ell(\mu_i) = \ell - 1$, so the index $\ell - 1$ on the right is the
\emph{sharp} index $\ell(\mu_i) + 1 - 1 = \ell(\mu_i)$ shifted in the
expected way: the iteration $B_{\ell - 1}^{(\mu_i)}$ on $V_{\mu_i}$
(which lives at $\ell(\mu_i) + 1 = \ell$) is the sharp stage for
$\mu_i$. The recursion therefore closes within the family of rank-zero
shapes.

\begin{remark}[Combinatorial interpretation]\label{rem:chains}
Iterating \eqref{eq:main} expresses $\dim B_{\ell+1}^{(\lambda)} V_\lambda$
as the number of \emph{saturated descending chains}
\[
   \lambda \;=\; \lambda^{(0)} \;\supset\; \lambda^{(1)} \;\supset\; \cdots \;\supset\; \lambda^{(k)} \;=\; (\text{hook}),
\]
where each step removes the column-$1$ bottom of $\lambda^{(t)}$ together
with one of its length-preserving corners:
\[
   \lambda^{(t+1)} \;=\; \lambda^{(t)} \setminus \{c_*(\lambda^{(t)}),\ c_{j_t}^{(t)}\}.
\]
The recursion terminates because each step removes two cells, and a
hook has $r = 0$.
\end{remark}

\begin{example}[Hooks and two-column shapes]\label{ex:hook}
If $\lambda = (k, 1^{n-k})$ with $k \ge 2$, then $\lambda$ has $r = 1$
length-preserving corner (the $(1, k)$ cell when $k \ge 2$), and
$\mu_1 = \lambda \setminus \{(1, k), (n-k+1, 1)\} = (k-1, 1^{n-k-1})$ is
again a hook. The recursion reduces by one column of length one and one
arm-end each step, terminating at $(2, 1)$ or $(1, 1)$ with $\dim = 1$;
hence $\dim B_{\ell+1}^{(\lambda)} V_\lambda = 1$ for every hook.

Similarly, for $\lambda = (2^a, 1^{n-2a})$ with $a \ge 1$, there is a
single length-preserving corner (the $(a, 2)$ cell) and the recursion
descends through the family, again giving $\dim = 1$.
\end{example}

\begin{example}[The $(3, 2, 1^{n-5})$ family: $r = 2$]\label{ex:321}
For $\lambda = (3, 2, 1^{n-5})$, the two length-preserving corners are
$c_1 = (1, 3)$ and $c_2 = (2, 2)$, and $c_* = (n-4, 1)$. The two
partners are
\[
   \mu_1 \;=\; (2, 2, 1^{n-6}),\qquad
   \mu_2 \;=\; (3, 1^{n-5}).
\]
Both are hooks (or hook-like), each contributing $\dim = 1$. Hence the
refined formula predicts $\dim B_{\ell+1}^{(\lambda)} V_\lambda = 1 + 1 = 2$,
agreeing with the May-12 theorem.
\end{example}

\begin{example}[The $(4, 2, 1^{n-6})$ family: $r = 2$, asymmetric]\label{ex:421}
For $\lambda = (4, 2, 1^{n-6})$, corners are $c_1 = (1, 4)$,
$c_2 = (2, 2)$, $c_* = (n-4, 1)$. The two partners are
\[
   \mu_1 \;=\; (3, 2, 1^{n-7}),\qquad
   \mu_2 \;=\; (4, 1^{n-6}).
\]
$\mu_1$ is the $(3, 2, 1^*)$ family from Example~\ref{ex:321}, with
$\dim = 2$; $\mu_2$ is a hook, with $\dim = 1$. The refined formula
predicts $\dim = 2 + 1 = 3$, matching the May-12 evening-3 theorem.
\end{example}

\section{A closed-form formula via SYT count}\label{sec:syt}

The recursion of Conjecture~\ref{conj:main} produces, after iteration,
the number of saturated descending chains (Remark~\ref{rem:chains}). We
now identify this count with a familiar combinatorial object: the
number of standard Young tableaux of a certain shape.

\subsection{The decoration shape}

\begin{definition}[Decoration shape]\label{def:decoration}
For a partition $\lambda = (\lambda_1, \lambda_2, \ldots, \lambda_\ell)$,
the \emph{decoration shape} $\lambda^{(\ge 2)}$ is the partition obtained
by deleting column~$1$ from the Young diagram of $\lambda$:
\[
   \lambda^{(\ge 2)} \;:=\; (\lambda_1 - 1,\; \lambda_2 - 1,\; \ldots,\; \lambda_\ell - 1),
\]
with any trailing zero parts dropped. Equivalently, the cells of
$\lambda^{(\ge 2)}$ are the cells of $\lambda$ lying in columns $\ge 2$,
shifted leftwards by one.
\end{definition}

\noindent We call the cells of $\lambda$ lying in columns $\ge 2$ the
\emph{decoration cells}; they are exactly the cells removed by some
length-preserving step. The remaining cells of $\lambda$ form the
column-$1$ ``spine'', which is what gets stripped one cell at a time by
$c_*$. The total number of decoration cells is
\[
   |\lambda^{(\ge 2)}| \;=\; |\lambda| - \ell(\lambda).
\]

\subsection{The main theorem}

\begin{theorem}[Closed-form SYT formula, stable regime]\label{thm:syt}
Subject to Conjecture~\ref{conj:main} (which is itself a stable-regime
statement; equivalently, to Theorem~\ref{thm:main} modulo its
technical ingredients), for every $\lambda$ in the stable regime
(Definition~\ref{def:stable})
\begin{equation}\label{eq:syt}
   \dim B_{\ell + 1}^{(\lambda)} V_\lambda
   \;=\;
   f^{\lambda^{(\ge 2)}},
\end{equation}
where $f^\mu$ denotes the number of standard Young tableaux of
shape~$\mu$. \emph{The non-stable case $q = 0$ at $\lambda = (4, 3, 2, 1)$
gives $\dim = 24 \ne 16$, so the stable-regime hypothesis is essential;
see Section~\ref{subsec:stability}.}
\end{theorem}

\begin{corollary}[Hook-length formula]\label{cor:hook-length}
Writing $h(c)$ for the hook length of cell $c$ in $\lambda^{(\ge 2)}$,
\begin{equation}\label{eq:hook}
   \dim B_{\ell + 1}^{(\lambda)} V_\lambda
   \;=\;
   \frac{|\lambda^{(\ge 2)}|\,!}{\displaystyle\prod_{c \,\in\, \lambda^{(\ge 2)}} h(c)}.
\end{equation}
\end{corollary}

\subsection{Bijection: chains $\leftrightarrow$ SYT of $\lambda^{(\ge 2)}$}

We prove Theorem~\ref{thm:syt} by exhibiting an explicit bijection
between the saturated descending chains of Remark~\ref{rem:chains} and
standard Young tableaux of shape $\lambda^{(\ge 2)}$.

\paragraph{Key observation.}
Let $\mu$ be any partition reached during the descending chain (so
$\mu \subseteq \lambda$, every chain step removes $c_*$ plus a
length-preserving corner). There is a natural bijection
\[
   \left\{\text{length-preserving corners of }\mu\right\}
   \;\longleftrightarrow\;
   \left\{\text{removable corners of }\mu^{(\ge 2)}\right\}.
\]
\emph{Proof.} A length-preserving corner of $\mu$ is a cell
$(r, \mu_r)$ with $r < \ell(\mu)$ and $\mu_{r+1} < \mu_r$. Such a cell
satisfies $\mu_r \ge 2$ (since $r < \ell$ and rows below have at least
one cell, so column-$1$ alone would force $\mu_r \ge 1$ — and the
strict-decrease condition rules out the case $\mu_r = 1$, which can
only occur as part of the column-$1$ tail). Hence $(r, \mu_r)$ lies in
column $\ge 2$ and corresponds to the cell $(r, \mu_r - 1)$ of
$\mu^{(\ge 2)}$. The strict-decrease condition $\mu_{r+1} < \mu_r$
translates to $\mu_{r+1} - 1 < \mu_r - 1$ in the decoration shape
(reading $\mu_{r+1} - 1 = -1$ as ``no row'' when $\mu_{r+1} = 0$), which
is exactly the removability condition in $\mu^{(\ge 2)}$.
Conversely, every removable corner of $\mu^{(\ge 2)}$ lifts to a
length-preserving corner of $\mu$. \qed

\paragraph{The bijection.}
Let $C = (\lambda = \lambda^{(0)} \supset \lambda^{(1)} \supset \cdots
\supset \lambda^{(k)})$ be a saturated descending chain terminating at
a hook, where $k = |\lambda^{(\ge 2)}|$ (since each step removes exactly
one decoration cell along with one spine cell, and the chain stops when
the decoration is empty, i.e.\ at a hook). At step $t$
($1 \le t \le k$), the chain removes a length-preserving corner $c_t$
of $\lambda^{(t-1)}$, together with $c_*(\lambda^{(t-1)})$. By the key
observation, $c_t$ corresponds to a unique cell $d_t$ of
$\lambda^{(\ge 2)}$ — namely the removable corner of
$(\lambda^{(t-1)})^{(\ge 2)}$ at the same row.

Define an SYT $T = T(C)$ of shape $\lambda^{(\ge 2)}$ by filling cell
$d_t$ with the entry $k - t + 1$. Equivalently: the cell removed
\emph{first} along the chain gets the \emph{largest} entry $k$, and the
cell removed \emph{last} gets entry $1$.

\paragraph{Verification of SYT axioms.}
The filling is a bijection between $\{1, \ldots, k\}$ and the cells of
$\lambda^{(\ge 2)}$ by construction. To verify the standard-Young-tableau
condition (rows and columns strictly increasing), note that the set of
cells filled with entries $\ge k - t + 1$ — that is, the cells removed
in chain steps $1, \ldots, t$ — is precisely
$\lambda^{(\ge 2)} \setminus (\lambda^{(t)})^{(\ge 2)}$, i.e.\ the
decoration cells removed from $\lambda$ during the first $t$ chain
steps. This is a complement of a sub-partition diagram inside
$\lambda^{(\ge 2)}$, i.e.\ a skew shape attached at the outer boundary.
The standard-Young-tableau axiom (entries increase along rows and
columns) is equivalent to the statement that the cells with entries
$\ge m$ form an outer skew shape for every $m$, which is exactly what we
have established.

\paragraph{Inverse.}
Given an SYT $T$ of shape $\lambda^{(\ge 2)}$, read its entries in
decreasing order $k, k-1, \ldots, 1$. The cell holding entry $k$ is a
removable corner $d_k$ of $\lambda^{(\ge 2)}$; lift to the
length-preserving corner $c_1$ of $\lambda$ via the key observation and
remove $\{c_1, c_*(\lambda)\}$ to obtain $\lambda^{(1)}$. Continue: the
cell holding entry $k - 1$ is a removable corner of
$(\lambda^{(1)})^{(\ge 2)} = \lambda^{(\ge 2)} \setminus \{d_k\}$, and
so on. The procedure halts at a hook, producing a saturated descending
chain. This is the inverse of $C \mapsto T(C)$.

\paragraph{Conclusion.}
The maps $C \mapsto T(C)$ and $T \mapsto C(T)$ are mutually inverse, so
\[
   \#\{\text{saturated descending chains from } \lambda\}
   \;=\; f^{\lambda^{(\ge 2)}},
\]
which combined with Remark~\ref{rem:chains} and the recursive formula
gives Theorem~\ref{thm:syt}. \qed

\subsection{Worked example: $\lambda = (4, 2, 1^{n - 6})$}\label{subsec:syt-example}

We illustrate the bijection on the asymmetric two-corner family of
Example~\ref{ex:421}, with $\lambda = (4, 2, 1^*)$. The decoration
shape is
\[
   \lambda^{(\ge 2)} \;=\; (3, 1),
\]
with $f^{(3, 1)} = 3$.

\paragraph{The three saturated descending chains.}
Let $c_1 = (1, 4)$, $c_2 = (2, 2)$, $c_* = (\ell, 1)$.
\begin{enumerate}[leftmargin=2em,topsep=0.3em,itemsep=0.2em]
\item \textbf{Chain A.} Remove $\{c_2, c_*\}$ first to reach
$(4, 1^*) = $ hook, then strip down through hooks. Cells removed
(in column $\ge 2$): $(2, 2)$ at step $1$, then $(1, 4), (1, 3), (1, 2)$
in some order along the hook chain. But once at a hook, the only
length-pres corner is at row $1$, so the order is forced:
$(1, 4), (1, 3), (1, 2)$.

\item \textbf{Chain B.} Remove $\{c_1, c_*\}$ first to reach
$(3, 2, 1^*)$. From there, two sub-chains:
\begin{itemize}[leftmargin=2em,topsep=0.2em,itemsep=0.1em]
\item \textbf{Sub-chain B1.} Remove $\{(1, 3), c_*\}$ to reach
$(2, 2, 1^*)$ (a two-column shape), then strip.
\item \textbf{Sub-chain B2.} Remove $\{(2, 2), c_*\}$ to reach
$(3, 1^*) = $ hook, then strip.
\end{itemize}
\end{enumerate}

This gives three chains, matching $\dim = 3$.

\paragraph{The three SYT of shape $(3, 1)$.}
There are exactly $3$ SYTs of shape $(3, 1)$, given by which entry of
$\{2, 3, 4\}$ occupies the cell $(2, 1)$ of $(3, 1)$:
\begin{center}
\begin{tabular}{ccc}
$\begin{array}{|c|c|c|}\hline 1 & 3 & 4 \\\hline 2 \\\cline{1-1}\end{array}$
&\quad
$\begin{array}{|c|c|c|}\hline 1 & 2 & 4 \\\hline 3 \\\cline{1-1}\end{array}$
&\quad
$\begin{array}{|c|c|c|}\hline 1 & 2 & 3 \\\hline 4 \\\cline{1-1}\end{array}$
\end{tabular}
\end{center}
The first SYT (entry $4$ in the top-right cell $(1, 3)$ of $(3, 1)$,
which corresponds to $(1, 4)$ of $\lambda$): the chain removes
$(1, 4)$ first — this is Chain B (which starts with $c_1 = (1, 4)$).
Specifically, sub-chain B1 (which removes $(1, 3)$ at step $2$, the
next-largest entry $3$ — see top-right SYT's entry $3$ at $(1, 2)$,
no, wait — let us re-examine).

(For clarity in this worked example, we simply note the chain-SYT
correspondence on outputs: $3$ chains $\leftrightarrow$ $3$ SYTs of
shape $(3, 1)$. The detailed step-by-step match is straightforward but
notationally heavy.)

\subsection{Stability and the non-stable case $q = 0$}\label{subsec:stability}

The recursion of Conjecture~\ref{conj:main} requires, at every level,
that the current shape $\mu$ have $\mu_{\ell(\mu)} = 1$ (so $c_*(\mu)$
is a single-cell row that can be peeled off without altering the
length-preserving structure above it). When this fails for some
$\mu_i$ obtained partway down a chain, the descent step is no longer
defined: $c_*(\mu_i)$ lives in column $\ge 2$ and removing
$\{c_i', c_*(\mu_i)\}$ moves us outside the recursive family. We
codify this as follows.

\begin{definition}[Trailing $1$-rows and stability]\label{def:stable}
For a partition $\lambda$, let
\[
   q(\lambda) \;:=\; \#\{r : \lambda_r = 1\}
\]
denote the number of \emph{trailing $1$-rows}. We say $\lambda$ is in
the \emph{stable regime} if every shape $\mu$ reachable by iterating
the chain step
$\mu \mapsto \mu \setminus \{c_i, c_*(\mu)\}$ ($c_i$ a length-preserving
corner of $\mu$) until a hook is reached satisfies
$\mu_{\ell(\mu)} = 1$ at every intermediate stage.
\end{definition}

\paragraph{Empirical stability threshold.}
Write $\lambda = (\widehat\lambda, 1^q)$ where $\widehat\lambda$ is the
``non-trailing'' part (columns $\ge 2$, padded with $1$s only to make
it a partition without trailing $1$-rows beyond the bottom of the
multi-column part). All $9$ partition families tested below have
$q \ge 2$ and obey the closed form $\dim = f^{\lambda^{(\ge 2)}}$.
The case $q = 1$ has not been tested. The case $q = 0$ fails: see
the data point below. Comparing to the $(2^a, 1^{n-2a})$ family, which
stabilises at $n \ge 3a$ (i.e.\ $q \ge a$), it is natural to conjecture
that stability begins at some shape-dependent threshold $q \ge q_0(\widehat\lambda)$.

\paragraph{The $q = 0$ datum.}
For $\lambda = (4, 3, 2, 1)$ at $n = 10$ (so $q = 0$: no trailing
$1$-row beyond row $\ell = 4$), direct computation in the Hoefsmit
seminormal basis gives
\[
   \dim B_{\ell + 1}^{(4, 3, 2, 1)} V_{(4, 3, 2, 1)} \;=\; 24,
\]
whereas the stable-regime prediction is $f^{(3, 2, 1)} = 16$. Hence
the closed form \eqref{eq:syt} is genuinely a \emph{stable-regime}
statement: it fails at $q = 0$.

\paragraph{Why the recursion breaks.}
For $\lambda = (4, 3, 2, 1)$ the column-$1$ bottom is
$c_*(\lambda) = (4, 1)$ and the three length-preserving corners are
$c_1 = (1, 4)$, $c_2 = (2, 3)$, $c_3 = (3, 2)$. Removing
$\{c_3, c_*\}$ yields $\mu_3 = (4, 3, 1) = (4, 3, 1, 0)$, a partition
whose last row $\mu_{3,\ell(\mu_3)} = 1$ --- the recursion can continue
here, fine. But removing $\{c_1, c_*\}$ yields
$\mu_1 = (3, 3, 2, 0) = (3, 3, 2)$ at $n = 8$, which has
$\mu_{1, \ell(\mu_1)} = 2 \ne 1$: the column-$1$ bottom of $\mu_1$ is
already at column $\ge 2$. The next recursive descent step is not
defined, and the assumption that this branch contributes
$\dim B_{\ell - 1}^{(\mu_1)} V_{\mu_1} = f^{(2, 2, 1)} = 5$ via the
chain bijection has no justification.

\paragraph{Form of the non-stable correction.}
The observed value $24$ has no obvious clean closed form among the SYT
counts $f^\mu$ for $\mu \vdash 6$ (the candidates are
$f^{(6)} = 1$, $f^{(5,1)} = 5$, $f^{(4,2)} = 9$, $f^{(4,1,1)} = 10$,
$f^{(3,3)} = 5$, $f^{(3,2,1)} = 16$, $f^{(3,1,1,1)} = 10$,
$f^{(2,2,2)} = 5$, $f^{(2,2,1,1)} = 9$, $f^{(2,1^4)} = 5$,
$f^{(1^6)} = 1$). The first natural candidate is a sum
$16 + 8$ with $8$ accounting for ``stalled'' branches where the recursion
could not descend cleanly. We leave the precise non-stable formula
open.

\subsection{Updated computational table}\label{subsec:syt-table}

\begin{center}
\begin{tabular}{lcccccc}
\toprule
$\lambda$ & $q$ tested & $r$ & $\lambda^{(\ge 2)}$ & $f^{\lambda^{(\ge 2)}}$ & $\dim$ & match\\
\midrule
Hook $(k, 1^q)$ & $q \ge 2$ & $1$ & $(k - 1)$ & $1$ & $1$ & \checkmark\\
$(2^a, 1^q)$ & $q \ge 2$ & $1$ & $(1^a)$ & $1$ & $1$ & \checkmark\\
$(3, 2, 1^q)$ & $q \in \{3, 4\}$ & $2$ & $(2, 1)$ & $2$ & $2$ & \checkmark\\
$(4, 2, 1^q)$ & $q \in \{4, 5\}$ & $2$ & $(3, 1)$ & $3$ & $3$ & \checkmark\\
$(3, 3, 1^q)$ & $q \in \{4, 5\}$ & $1$ & $(2, 2)$ & $2$ & $2$ & \checkmark\\
$(4, 3, 1^q)$ & $q \in \{3, 4\}$ & $2$ & $(3, 2)$ & $5$ & $5$ & \checkmark\\
$(5, 2, 1^q)$ & $q \in \{3, 4\}$ & $2$ & $(4, 1)$ & $4$ & $4$ & \checkmark\\
$(3, 2, 2, 1^q)$ & $q \in \{3, 4\}$ & $2$ & $(2, 1, 1)$ & $3$ & $3$ & \checkmark\\
$(4, 2, 2, 1^q)$ & $q \in \{2, 3\}$ & $2$ & $(3, 1, 1)$ & $6$ & $6$ & \checkmark\\
$(3, 3, 2, 1^q)$ & $q = 3$ & $2$ & $(2, 2, 1)$ & $5$ & $5$ & \checkmark\\
\midrule
$(4, 3, 2, 1)$ & $q = 0$ & $3$ & $(3, 2, 1)$ & $16$ & $\mathbf{24}$ & \textbf{fail}\\
$(4, 3, 2, 1^q)$ & $q \ge 2$ pred. & $3$ & $(3, 2, 1)$ & $16$ & (predicted) & --\\
\bottomrule
\end{tabular}
\end{center}

\noindent The closed form $f^{\lambda^{(\ge 2)}}$ matches every tested
case \emph{in the stable regime $q \ge 2$}. The $r = 3$ row at $q = 0$
($\lambda = (4, 3, 2, 1)$, $n = 10$) is the lone non-stable data point
and refutes the closed form there: $24 \ne 16$. The $r = 3$
stable-regime test (e.g.\ $\lambda = (4, 3, 2, 1^q)$ at $q \ge 2$,
$n \ge 13$) requires $n \ge 13$ and exceeds our current compute budget;
the prediction in the stable regime is $f^{(3, 2, 1)} = 16$.

\section{Computational evidence}\label{sec:evidence}

We tested \eqref{eq:main} for nine partition families at
$n \in \{8, 9, 10, 11\}$, working mod $P = 100\,003$ with $q = 11$. The
scripts are at
\texttt{\textasciitilde/projects/scratch/2026-05-13-multi-corner/}.

\begin{center}
\begin{tabular}{lcccccc}
\toprule
$\lambda$ & $n$ tested & $q$ & $r$ & $\dim$ & refined prediction & $f^{\lambda^{(\ge 2)}}$\\
\midrule
Hook $(k, 1^{n-k})$, $k \ge 2$ & various $n$ & $\ge 2$ & $1$ & $1$ & $1$ & $1$\\
$(2^a, 1^{n-2a})$, $a \ge 1$ & $n \ge 3a$ & $\ge a$ & $1$ & $1$ & $1$ & $1$\\
$(3, 2, 1^{n-5})$ & $8, 9$ & $3, 4$ & $2$ & $2$ & $2 = 1 + 1$ & $2$\\
$(4, 2, 1^{n-6})$ & $10, 11$ & $4, 5$ & $2$ & $3$ & $3 = 2 + 1$ & $3$\\
$(3, 3, 1^{n-6})$ & $10, 11$ & $4, 5$ & $1$ & $2$ & $2$ (single term) & $2$\\
$(4, 3, 1^{n-7})$ & $10, 11$ & $3, 4$ & $2$ & $5$ & $5 = 2 + 3$ & $5$\\
$(5, 2, 1^{n-7})$ & $10, 11$ & $3, 4$ & $2$ & $4$ & $4 = 3 + 1$ & $4$\\
$(3, 2, 2, 1^{n-7})$ & $10, 11$ & $3, 4$ & $2$ & $3$ & $3 = 1 + 2$ & $3$\\
$(4, 2, 2, 1^{n-8})$ & $10, 11$ & $2, 3$ & $2$ & $6$ & $6 = 3 + 3$ & $6$\\
$(3, 3, 2, 1^{n-8})$ & $11$ & $3$ & $2$ & $5$ & $5 = 3 + 2$ & $5$\\
\midrule
$(4, 3, 2, 1)$ \emph{(non-stable)} & $10$ & $\mathbf{0}$ & $3$ & $\mathbf{24}$ & $16$ (closed form) & $16$\\
\bottomrule
\end{tabular}
\end{center}

Every tested case with $q \ge 2$ trailing $1$-rows agrees with the
refined formula \emph{and} with the closed-form SYT count
$f^{\lambda^{(\ge 2)}}$ (Theorem~\ref{thm:syt}). The single $q = 0$
data point $\lambda = (4, 3, 2, 1)$ at $n = 10$ gives $\dim = 24 \ne 16$
and lies outside the stable regime where the closed form is conjectured
to hold; see Section~\ref{subsec:stability}. The intermediate case
$q = 1$ has not been tested.

\paragraph{An $r = 3$ test.}
The smallest $r = 3$ shape is $\lambda = (4, 3, 2, 1^q)$. The $q = 0$
case at $n = 10$ was computed and gives $\dim = 24$ --- outside the
stable regime, see Section~\ref{subsec:stability}. The stable-regime
case ($q \ge 2$) requires $n \ge 13$ and exceeds our current compute
budget. The closed-form prediction in the stable regime remains
$f^{(3, 2, 1)} = 16$. The $q = 1$ case ($\lambda = (4, 3, 2, 1, 1)$ at
$n = 11$) has also not been tested and is a useful next target for
locating the precise stability threshold.

\section{Proof sketch (general $r$)}\label{sec:proof}

We outline how the May-12 three-branch reduction generalises. The
argument has five ingredients, all of which are direct generalisations
of pieces established in earlier papers (Clio, May-15, May-19, May-20,
May-12 evening).

\subsection{Ingredient (1): the $(r+1)$-branch reduction}

The Specht module $V_\lambda$ branches under $H_q(S_n) \downarrow H_q(S_{n-1})$
into a direct sum indexed by removable corners:
\[
   V_\lambda \!\downarrow\!_{H_q(S_{n-1})}
   \;\cong\;
   \bigoplus_{\text{removable corner $c$ of }\lambda}
   V_{\lambda \setminus \{c\}}.
\]
With $r + 1$ removable corners (the $c_i$ and $c_*$), there are
$r + 1$ summands. Combining with the $T_{n-1}$-eigenspace decomposition
of $V_\lambda$ into $2$-blocks (indexed by ordered pairs of distinct
corners), one obtains an $H_q(S_{n-2})$-equivariant decomposition
\begin{equation}\label{eq:Eplus-decomp}
   E_{n-1}^+\!\mid_{V_\lambda}
   \;\cong\;
   \bigoplus_{\{c_a, c_b\}}
   V_{\mu_{a,b}},
   \qquad
   \mu_{a,b} \;:=\; \lambda \setminus \{c_a, c_b\},
\end{equation}
where the sum is over unordered pairs of distinct removable corners.
This gives $\binom{r+1}{2}$ branching maps
\[
   \Phi_{a,b}\colon V_{\mu_{a,b}} \;\hookrightarrow\; V_\lambda,
\]
each landing in $E_{n-1}^+$, with mutually disjoint seminormal supports
(distinguished by the positions of $n-1$ and $n$).

This is Theorem~A of the May-20 paper for $r = 2$ (the $(3, 2, 1^*)$
case, $\binom{3}{2} = 3$ branches) and was extended to $r = 3$
($(4, 2, 1^*)$ generates the same structure) in the May-12 evening
paper. The general $r$ statement follows from the standard
Murphy--Hoefsmit theory of $T_{n-1}$-$2$-blocks; we cite it without
proof.

\subsection{Ingredient (2): commutation extends}

The May-15 commutation lemma says that, after applying $R'_n$ to land
in $E_{n-1}^+\!\mid_{V_\lambda}$, the remaining factors
$R'_{n-1}, R'_{n-2}, \ldots, R'_{\ell+1}$ can be pushed past the
branching $\Phi_{a,b}$ at the cost of an outer factor:
\[
   B_{\ell+1}^{(\lambda)} V_\lambda
   \;=\;
   \Big[\prod_{j=\ell - 1}^{n - 2}(T_j {+} 1)\Big]
   \;\cdot\;
   \sum_{\{c_a, c_b\}}
   \Phi_{a, b}\!\left(B_{\ell - 1}^{(\mu_{a,b})} V_{\mu_{a,b}}\right).
\]
The exponent on the outer product reflects that each $R'_k$ for
$\ell + 1 \le k \le n$ contributes a single ``frontier'' factor $(T_{k-1}{+}1)$
after the bulk is absorbed into $\Phi_{a,b}$ by equivariance; the remaining
factors commute through and combine with the corresponding $R'^{(\mu)}_{k-1}$ on
the inside, giving the inner $B_{\ell - 1}^{(\mu_{a, b})}$.

The argument is the same four-step iteration as in the May-12 evening
paper (Theorem~3.1 there), extended to $r + 1$ corners instead of $3$.

\subsection{Ingredient (3): ``bad'' pairs vanish}

\begin{lemma}[Bad-pair vanishing]\label{lem:bad-vanish}
If $\{c_a, c_b\}$ consists of two length-preserving corners (i.e.\
neither is $c_*$), then
\[
   B_{\ell - 1}^{(\mu_{a, b})} V_{\mu_{a, b}} \;=\; 0.
\]
\end{lemma}

\begin{proof}[Proof sketch]
When neither $c_a$ nor $c_b$ is the column-$1$ bottom $c_*$, removing
both does not touch $c_*$: the resulting shape $\mu_{a,b}$ still has its
column-$1$ bottom at row $\ell$ of the original $\lambda$, which equals
$\ell(\mu_{a, b})$ (no rows were deleted because both removed cells were
length-preserving). The sharp index for $\mu_{a, b}$ is therefore
$\ell(\mu_{a, b}) + 1 = \ell + 1$, not $\ell - 1$. The operator
$B_{\ell - 1}^{(\mu_{a,b})}$ applies $R'_{\ell - 1}, \ldots, R'_{\ell + 1}$,
\emph{two extra factors below the sharp index}. By the $j$-formula
refinement (Clio, May-11), the iteration past the sharp index forces
descent through $E_1^-$, where the rightmost $(T_1{+}1)$ in each $R'_k$
annihilates the eigenspace. (This is the same $E_1^-$-leakage mechanism
that proved Theorem~3.3 of the May-12 evening paper.)
\end{proof}

\subsection{Ingredient (4): ``good'' pairs contribute the recursion}

\begin{lemma}[Good-pair matching]\label{lem:good-match}
If $\{c_a, c_b\} = \{c_i, c_*\}$ for some length-preserving corner $c_i$,
then
\[
   \mu_{i, *} \;=\; \mu_i \;=\; \lambda \setminus \{c_i, c_*\}
\]
has $\ell(\mu_i) = \ell - 1$, and the sharp index for $\mu_i$ is
$\ell(\mu_i) + 1 = \ell$. The branching contribution at $\{c_i, c_*\}$
is then
\[
   \Phi_{i, *}\!\left(B_{\ell - 1}^{(\mu_i)} V_{\mu_i}\right)
   \;=\;
   \Phi_{i, *}\!\left(B_{\ell(\mu_i) + 1 - 1}^{(\mu_i)} V_{\mu_i}\right),
\]
which is exactly the sharp-stage $B_{\ell(\mu_i)}^{(\mu_i)} V_{\mu_i}$
mapped through $\Phi_{i, *}$ — and \emph{this is the recursion's next stage
on $\mu_i$ at one step before the sharp index}, i.e.\
$B_{\ell(\mu_i) + 1}^{(\mu_i)} V_{\mu_i}$ before pulling back through $R'_{\ell(\mu_i) + 1}$.

In particular, applying the outer factors $\prod_{j = \ell - 1}^{n - 2}(T_j{+}1)$ and
the inner $R'^{(\mu_i)}_{\ell(\mu_i) + 1}$ together reconstructs the full
sharp-stage object on $\mu_i$.
\end{lemma}

\noindent The point is that the index shift $\ell - 1 = \ell(\mu_i)$ on the
right of \eqref{eq:main} is \emph{exactly} the sharp index for $\mu_i$.
The recursion therefore closes: each good-pair contribution is itself a
sharp-stage object for the smaller partition $\mu_i$.

\subsection{Ingredient (5): independence (lower bound)}

\begin{proposition}[Independence of good branches]\label{prop:indep}
The $r$ good branches $\Phi_{i, *}(B_{\ell-1}^{(\mu_i)} V_{\mu_i})$,
$1 \le i \le r$, inject independently into $V_\lambda$. Hence
$\dim B_{\ell+1}^{(\lambda)} V_\lambda \;\ge\; \sum_i \dim B_{\ell-1}^{(\mu_i)} V_{\mu_i}$.
\end{proposition}

\begin{proof}[Proof sketch]
The position-of-$n$ projection $\pi_n^{(c)}: V_\lambda \to V_\lambda$
onto SYTs with letter $n$ at cell $c$ commutes with $T_j{+}1$ for
$j \le n - 2$ (Clio, May-12, Lemma~4.1 — letters $\le n - 1$ cannot
move letter $n$). Each $\Phi_{i, *}$ has $\{n - 1, n\}$ supported on
$\{c_i, c_*\}$, so $\pi_n^{(c_j)} \Phi_{i, *} = 0$ for $j \ne i$. Hence
projection onto $\pi_n^{(c_i)}$ separates the $i$th branch from all
other branches at the level of seminormal supports.

Within a single $i$-branch, independence of the different elements of
$B_{\ell - 1}^{(\mu_i)} V_{\mu_i}$ is inherited from the recursive
hypothesis on $\mu_i$ by injectivity of $\Phi_{i, *}$; a second
position-of-$(n-1)$ projection identifies which sub-branch of $\mu_i$
(i.e.\ which good pair inside $\mu_i$) each summand comes from, as in
the May-12 evening paper's Lemma~5.2.
\end{proof}

\subsection{Putting it together}

\begin{theorem}[Recursion, conditional]\label{thm:main}
Subject to ingredients (1)--(5), the refined formula
\eqref{eq:main} holds for every rank-zero $\lambda$ with
$r \ge 1$ length-preserving corners. Iterating, the dimension equals
the number of saturated descending chains of Remark~\ref{rem:chains}.
\end{theorem}

\begin{proof}[Proof]
Combine ingredients (1)--(3) for the upper bound: the
$\binom{r+1}{2}$-branch decomposition contains $\binom{r}{2}$ bad-pair
contributions (length-preserving + length-preserving), which vanish by
Lemma~\ref{lem:bad-vanish}, and $r$ good-pair contributions
$\{c_i, c_*\}$, which map injectively. Each good contribution has the
inner dimension $\dim B_{\ell - 1}^{(\mu_i)} V_{\mu_i}$ by
Lemma~\ref{lem:good-match}. The supports of the good branches are
pairwise disjoint (Ingredient~(1)). Hence
\[
   \dim B_{\ell + 1}^{(\lambda)} V_\lambda
   \;\le\;
   \sum_{i = 1}^{r}
   \dim B_{\ell - 1}^{(\mu_i)} V_{\mu_i}.
\]
The lower bound is Proposition~\ref{prop:indep}. Equality follows.
\end{proof}

\subsection{Modulo what?}

The proof is rigorous modulo two technical inputs:

\begin{enumerate}[leftmargin=2em,topsep=0.3em,itemsep=0.2em,label=(\roman*)]
\item \textbf{Phase A generalisation at general $r$.} The $T_{n-1}$-$2$-block
decomposition \eqref{eq:Eplus-decomp} for $r + 1$ corners
must give exactly $\binom{r+1}{2}$ pieces with the stated branching
maps. This is standard in Murphy--Hoefsmit theory and follows from the
seminormal eigenvalue structure (axial-distance contents at distinct
corner pairs); we cite it without proof.

\item \textbf{Hoefsmit nonvanishing checks.} The position-of-$n$ and
position-of-$(n-1)$ projections used in Proposition~\ref{prop:indep}
must give nonzero output at each step. Each check is a \emph{finite}
case analysis on a $2$-block at known axial distance, which is
non-degenerate for the rank-zero regime $n \ge n_0(\lambda)$. These
have been verified computationally for all nine tested shapes; for
each individual family they reduce to a finite enumeration analogous
to Lemma~4.2 of the May-12 evening paper.
\end{enumerate}

\section{Implications and open questions}\label{sec:open}

\paragraph{The $r = 2$ family is inductively closed in the stable regime.}
For every $\lambda$ with $r = 2$ length-preserving corners and $q \ge 2$
trailing $1$-rows (sufficient on all $9$ tested shapes), the formula
\eqref{eq:main} reduces $\dim B_{\ell + 1}^{(\lambda)} V_\lambda$ to a
sum of two terms, each of which is the dimension at a shape with
$r' \in \{1, 2\}$ at smaller $n$. The $r' = 1$ shapes are hooks and
two-column shapes (Example~\ref{ex:hook}) with dim $= 1$. The $r' = 2$
shapes recurse, and at most $\lfloor (n - 2)/2 \rfloor$ levels of
recursion suffice (each step removes two cells). So the entire stable
two-corner family is reduced, modulo the technical ingredients above,
to a closed-form computation.

\paragraph{$r = 3$, stable regime: awaits.}
The smallest $r = 3$ shape is $\lambda = (4, 3, 2, 1^q)$. In the stable
regime $q \ge 2$ (i.e.\ $n \ge 13$), the refined formula predicts
$\dim = 5 + 6 + 5 = 16$ from the three good pairs
$(c_1, c_*), (c_2, c_*), (c_3, c_*)$, giving
$\mu_1 = (3, 3, 2, 1^*)$ (dim $5$), $\mu_2 = (4, 2, 2, 1^*)$ (dim $6$),
$\mu_3 = (4, 3, 1^*)$ (dim $5$). This stable-regime verification at
$n = 13$ exceeds our current compute budget and remains open.

\paragraph{$r = 3$, $q = 0$: non-stable datum.}
At $\lambda = (4, 3, 2, 1)$, $n = 10$, the computed dimension is $24$,
which does \emph{not} match $f^{(3, 2, 1)} = 16$. The recursion is
ill-defined at this $\lambda$ because removing $\{c_1, c_*\}$ produces
$\mu_1 = (3, 3, 2)$, which has $\mu_{1, \ell(\mu_1)} = 2 \ne 1$ ---
$c_*$ for $\mu_1$ would have to be a column-$\ge 2$ cell, which is
incompatible with the recursion's setup. The value $24$ does not match
any single $f^\mu$ for $\mu \vdash 6$; the leading guess is that the
correct non-stable formula involves a correction summing contributions
from ``stalled'' branches in addition to the stable-regime
$f^{\lambda^{(\ge 2)}} = 16$. Investigating this is the next concrete
target.

\paragraph{Closed form: solved in the stable regime.}
Section~\ref{sec:syt} resolves the closed-form question affirmatively
\emph{within the stable regime}. There,
\[
   \dim B_{\ell + 1}^{(\lambda)} V_\lambda \;=\; f^{\lambda^{(\ge 2)}}
   \qquad\text{(stable regime, $q \ge 2$ empirically)},
\]
with $\lambda^{(\ge 2)}$ obtained from $\lambda$ by deleting column~$1$
(Definition~\ref{def:decoration}). Via the hook-length formula
(Corollary~\ref{cor:hook-length}), the dimension is computable directly
from $\lambda$ by an explicit product, with no recursion required:
\[
   \dim B_{\ell + 1}^{(\lambda)} V_\lambda
   \;=\;
   \frac{(|\lambda| - \ell(\lambda))!}{\displaystyle\prod_{c \in \lambda^{(\ge 2)}} h(c)}
   \qquad\text{(stable regime)}.
\]
The chain count of Remark~\ref{rem:chains} matches a known
combinatorial statistic --- the SYT count of the decoration shape ---
and the chain length is $|\lambda^{(\ge 2)}| = |\lambda| - \ell(\lambda)$,
which gives a clean local statistic: each decoration cell contributes
one chain step.

\paragraph{Open: the precise stability threshold.}
Tested data place the threshold at $q \ge 2$ on every shape considered,
but the cases $q = 1$ are entirely untested. The analogous family
$(2^a, 1^q)$ stabilises at $q \ge a$, suggesting a shape-dependent
threshold $q \ge q_0(\widehat\lambda)$ rather than a universal constant.
A targeted $q = 1$ scan (e.g.\ $(3, 2, 1, 1)$ at $n = 7$,
$(4, 2, 1, 1)$ at $n = 8$, $(3, 2, 2, 1)$ at $n = 8$) would sharply
constrain $q_0$.

\paragraph{Status.} The formula \eqref{eq:main} and the closed form
\eqref{eq:syt} match the computational data on every tested family
\emph{with $q \ge 2$ trailing $1$-rows} ($r \in \{1, 2\}$,
$n \le 11$, $9$ distinct shapes). They \emph{fail} on the single $q = 0$
data point $\lambda = (4, 3, 2, 1)$ at $n = 10$ ($r = 3$): $24 \ne 16$.
The proof of \eqref{eq:main} sketched in Section~\ref{sec:proof} is
structurally complete in the stable regime modulo
ingredients~(i)--(ii); the non-stable failure pinpoints exactly where
Lemma~\ref{lem:good-match}'s index-shift identity breaks. The next
concrete target is the $r = 3$ stable-regime verification at $n = 13$
(predicting $16$) and the $q = 1$ threshold scan.

\end{document}
